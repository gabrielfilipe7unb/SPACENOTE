.wrapper_with_padding
	#notes.clearfix
		- unless @notes.blank?
			- @notes.each do |note|
				%a{ href: (url_for [note])}
					.note
						%p.title= note.title
						%p.date= time_ago_in_words(note.created_at)
		- else
			%h2 Adicione uma nova nota
			%p Você ainda não criou nenhuma nota... Vamos, coloque essas ideias para fora de sua cabeça!
			%button= link_to "Nova Nota", new_note_path
